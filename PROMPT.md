# ずんだもん＆四国めたん 解説動画ジェネレーター

## 動画テーマ（ここだけ編集する）

```
猫の雑学（猫がゴロゴロ鳴る理由、液体のように箱に入る理由など）
```

---

## Claude Code への指示（以下は編集不要）

上記テーマで、ずんだもん＆四国めたん の掛け合い解説動画を生成してください。
成果物は **単一の `index.html`** ファイルです。ブラウザで開くだけで再生できます。

### 全体仕様

- **形式**: HTML + CSS + JavaScript の単一ファイル（外部ライブラリ不使用）
- **画面**: 16:9 アスペクト比、最大幅960px
- **構成**: タイトル画面 → 「さいせい」ボタン → 掛け合い再生 → 終了
- **キャラクター画像**: `images/zundamon.png`（左）、`images/metan.png`（右）を使用
- **操作**: スペースキー/タップで一時停止・再開、プログレスバー表示

### キャラクター設定

| キャラ | 口調 | 役割 | VOICEVOX speaker ID |
|--------|------|------|---------------------|
| ずんだもん | 「〜なのだ」語尾 | メイン解説役・ボケ | 3 |
| 四国めたん | 「〜ですわ」「〜ですの」丁寧語 | ツッコミ・相槌 | 2 |

### 台本（SCRIPT配列）のルール

- 10〜16行程度の掛け合い。zunda と metan が交互またはテンポよく話す
- 各行は `{s: "zunda"|"metan", t: "字幕テキスト", v: "読み上げテキスト"}`
- `t`（字幕）: 画面表示用。長い行は `\n` で改行を入れて3行以内に収める
- `v`（読み上げ）: TTS に送るテキスト。改行なし、読みやすい自然な文
- 冒頭は自己紹介、末尾は「グッドボタン」「ばいばい」で締める
- テーマに合った豆知識・驚きの事実を2〜3個盛り込む

### 音声 TTS 仕様（重要 — 無料APIのみ使用）

**API**: tts.quest v3（無料・APIキー不要）
```
GET https://api.tts.quest/v3/voicevox/synthesis?speaker={id}&text={text}
```

**レスポンス処理の正しい実装:**

1. **HTTP 429（rate limit）**: レスポンスbody の `retryAfter` 秒待ってリトライ
2. **`audioStatusUrl` が返る場合**: 合成は非同期。以下の手順で処理する
   - `audioStatusUrl` をポーリングして `isAudioReady: true` を待つ
   - **注意: ステータスAPIのレスポンスに mp3DownloadUrl は含まれない**（`isAudioReady` と `isAudioError` のみ）
   - ready になったら、**最初の synthesis レスポンスに含まれていた `mp3DownloadUrl`** を使う
3. **`audioStatusUrl` がない場合**: `mp3DownloadUrl` または `mp3StreamingUrl` が即利用可能

**パフォーマンス最適化（必須）:**

- **直列API + 並列ポーリング**: API呼び出しは直列（rate limit回避）、ポーリングはバックグラウンドで並列実行。ポーリングでキューをブロックしない
- **ページ読み込み時に先読み開始**: 再生ボタン押下を待たず、ページロード時点で全音声の先読みをキューに積む
- **ダブルバッファリング**: 現在のセリフ再生中に次のセリフの mp3 を別の Audio 要素にプリロード（`.load()`）。`ended` 発火時にプリロード済み Audio に切り替えて即 `.play()`。セリフ間の遅延を最小化する
- **API呼び出し間隔**: 500ms（429を正しくハンドリングしているため短縮可能）
- **ポーリング間隔**: 500ms、最大30回

### UI/デザイン仕様

- **背景**: テーマに合ったグラデーション + Unicode絵文字をうっすら表示（opacity 0.06）
- **キャラクター**: 話しているキャラは明るく少し浮く、話していないキャラは暗くグレースケール
- **字幕エリア**: 画面下部30%、半透明黒背景、話者名タグ付き（ずんだもん=緑、四国めたん=ピンク）
- **コントロールバー**: 最下部20px、一時停止ボタン + プログレスバー + ステップ表示
- **タイトル画面**: テーマ名、キャラ名、クレジット（VOICEVOX + ttsQuestV3 へのリンク）、再生ボタン
- **レスポンシブ**: `clamp()` でフォントサイズ可変、モバイル対応

### 安全機構

- **安全タイマー**: 各ステップ最大15秒で次へ進む（API応答なし対策）
- **フォールバック**: 音声取得失敗時はテキスト長に応じた待ち時間後に次へ進む
- **世代管理 (gen)**: ステップ変更のたびにインクリメント、古い非同期処理の誤発火を防止

### 注意事項

- **有料サービスは一切使わない**。TTS は tts.quest 無料枠のみ。外部CDN・有料API・APIキー必須のサービス禁止
- VOICEVOX のクレジット表記は必須（利用規約）
- `images/zundamon.png` と `images/metan.png` は既存のものをそのまま使う（生成不要）
- 既存の `index.html` を上書きする形で生成すること
